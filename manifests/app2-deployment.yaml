apiVersion: apps/v1
kind: Deployment
metadata:
  name: app2
  namespace: task2-ns
  labels:
    app: app2
    app.kubernetes.io/name: app2
    app.kubernetes.io/part-of: task2-flask-suite
spec:
  replicas: 2
  revisionHistoryLimit: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: app2
  template:
    metadata:
      labels:
        app: app2
        app.kubernetes.io/name: app2
        app.kubernetes.io/part-of: task2-flask-suite
    spec:
      terminationGracePeriodSeconds: 20
      securityContext:
        seccompProfile:
          type: RuntimeDefault

      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: app2

      containers:
        - name: app2
          image: 866340886126.dkr.ecr.us-east-1.amazonaws.com/app2:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8082
              name: http
          env:
            - name: PORT
              value: "8082"
            - name: HEALTH_PATH
              value: "/health"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          # In app2-deployment.yaml (container spec section)
          readinessProbe:
            httpGet:
              path: /app2          # ← matches the actual route
              port: 8082
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /app2          # ← same route for simplicity
              port: 8082
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 2
            failureThreshold: 3